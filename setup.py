from setuptools import setup

setup(
   name='project',
   version='1.0',
   description='Loodspots list',
   packages=['project'],  #same as name
   install_requires=['flask'], #external packages as dependencies
)